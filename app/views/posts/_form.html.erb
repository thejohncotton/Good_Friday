<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :he_led_me_to %>
    <%= form.text_field :title, id: :post_title, placeholder: "Love my neighbor", required: "true" %>
  </div>

  <div class="field">
    <%= form.label :story %>
    <%= form.text_area :description, id: :post_description, placeholder: "....share the story.", , required: "true" %>
  </div>
<div>
    <%= form.submit 'Share', class:"button green wide ae-10 cropBottom" %> </br>
</div>
<% end %>
